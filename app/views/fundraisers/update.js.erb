<% if @fundraiser_profile.errors.present? %>
  alert("Make sure you've completed all the required fields.");
<% else %>
  $('.banner').html("<%=j image_tag(@fundraiser_profile.banner.medium.url, class: 'img-responsive img-thumbnail') %>");
<% end %>