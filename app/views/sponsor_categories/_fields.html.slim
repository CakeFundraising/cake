.nested-fields
  = f.inputs do
    .row.pledge_level
      .col-md-6
        = f.input :name, placeholder: 'i.e. Platinum'
      .col-md-6
        .input-group
          strong.min_value = f.object.decorate.min
          = f.input :min_value, as: :hidden
          span  to 
          strong $
          = f.input :max_value, label: false, input_html: {class:'max_value'}