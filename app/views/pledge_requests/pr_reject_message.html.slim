h2 Describe below why you are rejecting this offer.
= semantic_form_for :reject_message, url: reject_pledge_request_path(@pledge_request), method: :patch do |f|
  = f.inputs do
    = f.input :message, as: :text, input_html:{class: 'autogrow', rows: 10, cols: 20 }
    = f.actions do
      = f.action :submit, label: 'Send', button_html: {class: 'btn btn-success pull-right'}