- if @pledge.persisted?
  br
  = link_to 'Delete', @pledge, data: {confirm: 'Are you sure?'}, method: :delete, class:'btn btn-danger pull-right'
  = link_to "Preview", @pledge, class:'btn btn-primary pull-right'