= display_meta("Pledges")

h1.dash-head
	span Pledges
	.page-cta
		= link_to "Find a Campaign to Promote", search_commissions_path, class:'btn btn-primary btn-md'
ul.nav.nav-tabs
  li.active 
    a data-toggle="tab" href="#requested" = t('titles.empty_results.pledges.my_pledge_requests')
  li 
    a data-toggle="tab" href="#active" = t('titles.empty_results.pledges.active')
.tab-content
  #requested.tab-pane.active
    = render partial:'pledge_requests/tables/fundraiser/table', locals:{pledges: @requested_pledges}
  #active.tab-pane
    = render partial:'pledges/tables/active/fundraiser/table', locals:{pledges: @accepted_pledges}
